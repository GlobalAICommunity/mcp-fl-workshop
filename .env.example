# The event VM already contains this model. No API key or endpoint is required.
# Copy this file to `.env` only when the facilitator has cached another alias.
#
# The alias is hardware-independent. Workshop code selects its cached generic
# CPU variant for VM portability. An uncached model fails by design offline.
MCP_WORKSHOP_MODEL=qwen3.5-0.8b
